/**
 * PostgreSQL adapter target identifier shared by command, schema, and Layer
 * exports without forcing modules through the package barrel.
 */
export const postgresAdapterTarget = "effect-postgres" as const;
