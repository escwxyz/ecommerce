/**
 * PostgreSQL adapter package. The first implementation will compose
 * `@effect/sql-pg` with Drizzle's Effect PostgreSQL driver.
 */
export const postgresAdapterTarget = "effect-postgres" as const;
